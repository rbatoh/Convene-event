# API Reference

*This document outlines the request and response shapes for all backend API endpoints. It serves as a frontend-focused reference to avoid digging through infrastructure details.*

## Endpoints
- `POST /register`
- `GET /events`
- `GET /registrations/{email}`
- `DELETE /cancel/{registration_id}`
- `GET /admin/events` (Requires AWS IAM Auth)

*(Detailed payload schemas and status codes go here)*
