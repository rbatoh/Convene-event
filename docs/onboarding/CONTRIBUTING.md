# Contributing Guidelines

- Use branch names like `feature/foo` or `bugfix/bar`.
- Fill out the provided PR template.
- If making architecture changes, you must submit a new ADR.
- All code requires a passing CI build (linting and tests).
